name: wait-for-build
version: 0.2.0
description: Wait for a dependent build/deploy on another project to complete.
keywords:
  - build
  - deploy
  - wait
  - dependency
properties:
  application:
    type: string
    required: true
  owner:
    type: string
    required: true
  token:
    type: string
    required: true
  is_deploy:
    type: boolean
    default: false
